#+title: Chirp user guide
#+options: toc:2 num:nil

* Sign in

~M-x chirp-login~ opens or attaches a supported browser at
~https://x.com~.  Chirp uses the isolated root in
~chirp-x-browser-session-profile-root~, which defaults to
~(locate-user-emacs-file "chirp/browser-session/")~.  It imports only
the ~auth_token~ and ~ct0~ cookies into ~chirp-x-auth-file~
(~(locate-user-emacs-file "chirp/auth.json")~, mode ~0600~).

Ordinary commands then use that private auth file.  Run
~M-x chirp-login~ again only to refresh an expired session or switch
accounts.  Chirp never reads a browser cookie database, copied
headers, ~CHIRP_X_AUTH_TOKEN~, ~CHIRP_X_CT0~, or ~auth-source~.
~M-x chirp-forget-browser-session~ deletes the auth file without
signing out of the browser.

Configure the browser, proxy, or Firefox/Zen behavior through
browser-session's public customization variables before login.  Do
not point ~chirp-x-browser-session-profile-root~ at a daily-use
browser profile.  Set ~CHIRP_X_BEARER_TOKEN~ only if X rotates its
public web token.

After a definitive stale-query error, Chirp refreshes read-operation
query IDs from the current ~TwitterInternalAPIDocument~ registry.
Retry the failed read, or run ~M-x chirp-refresh-query-ids~.
~chirp-x-query-id-overrides~ remains authoritative.  Write-operation
IDs are never selected from the dynamic cache or retried.

Direct messages require the optional source-built Rust module.  Follow
[[file:../native/README.md][native/README.md]] and set
~chirp-xchat-native-module-file~ to its absolute path.  Other Chirp
features remain usable when it is absent.

* Entry points

| Command | Opens |
|---------+-------|
| ~M-x chirp-login~ | capture or refresh the X web session |
| ~M-x chirp-forget-browser-session~ | delete the imported auth file |
| ~M-x chirp-home~ | For You timeline |
| ~M-x chirp-following~ | Following timeline |
| ~M-x chirp-direct-messages~ | XChat inbox after one unlock |
| ~M-x chirp-bookmarks~ | bookmarks |
| ~M-x chirp-likes~ | the account's Likes |
| ~M-x chirp-me~ | the authenticated profile |
| ~M-x chirp-list~ | one of your lists, or a list by ID |
| ~M-x chirp-search~ | search |
| ~M-x chirp-thread~ | a thread by post ID |
| ~M-x chirp-profile~ | a user profile |
| ~M-x chirp-profile-followers~ | followers |
| ~M-x chirp-profile-following-users~ | following |
| ~M-x chirp-open-url~ | a supported ~x.com~ or ~twitter.com~ URL |

Commands that accept a post or list ID intentionally do not parse URLs. Use ~M-x chirp-open-url~ for a supported X or Twitter URL.

To route those links from ~browse-url~:

#+begin_src emacs-lisp
(add-to-list 'browse-url-handlers
             (cons chirp-url-browse-regexp #'chirp-open-url))
#+end_src

* Direct messages

** Session and synchronization

~M-x chirp-direct-messages~ performs one user-confirmed XChat unlock, then opens a recent-conversation inbox with participant avatars, latest-message previews, compact local times, and request or muted status trails.  ~RET~ or mouse-1 opens the current row.  ~M-x chirp-dm-cancel-unlock~ cancels active recovery and reports an uncertain outcome.

While the Chirp application remains active, one Appkit-owned XChat websocket receives bounded binary events, updates canonical conversations and open inboxes immediately, decrypts new messages for open conversations, sends the official keepalive, and reconnects after transport closure without retrying writes.  Conversation facts are shared across fresh views; each buffer keeps its own history window, position, reply context, and draft.

** Message interactions

Conversations decode signature-verified plain text through Appkit's semantic markup codec, and the composer encodes the same plain codec before native encryption.  Verified reactions appear as compact chips beneath their exact target message instead of becoming separate rows; the current account's reactions use a distinct blue chosen style.  Replies keep a view-local Appkit context card above the composer.

| Key | Action |
|-----+--------|
| ~RET~ / mouse-1 | activate an attachment or embedded post; otherwise move to the composer |
| ~r~ | reply to the message at point |
| ~!~ | add or remove a prompted reaction |
| ~N~ | fetch older message history.  Evil: ~g n~ |
| ~i~ | move to the composer and enter Evil insert state |
| ~C-c C-a~ | open the typed photo, GIF, video, audio, or file selector |
| ~C-c C-f~ | attach a document directly |
| ~C-c C-k~ | cancel the current reply context |
| ~RET~ in composer | send the captured draft |
| ~C-u RET~ | insert a composer newline |
| ~C-c C-c~ | send the captured draft |
| ~M-p~ / ~M-n~ | previous or next sent input |

** Attachments

*** Incoming attachments

Chirp downloads encrypted attachment bytes from X's TON media service and asks the native module to decrypt them with the exact verified conversation-key version named by the message.  Images, GIFs, and SVGs render inline.  Videos, audio, and documents expose native Emacs actions; attached posts render as Chirp tweet cards; other links remain explicit URL actions.

Decrypted files use ~chirp-cache-directory~, like other Chirp media.  Protect or clear that directory according to the local machine's confidentiality requirements.

*** Outgoing attachments

Use ~C-c C-a~ to choose a photo, GIF, video, audio file, or document; ~C-c C-f~ attaches a document directly.  Photos and GIFs keep inline draft previews, while the other kinds use typed cards.  The chosen kind belongs to the captured draft: native byte detection verifies photo, GIF, video, and audio selections, while the document kind intentionally accepts any file.

On send, the native module validates and encrypts each file under the conversation's latest verified key.  Chirp then initializes the upload, sends bounded encrypted parts to TON, finalizes it, and encrypts the message envelope under the same key version.  Uploads use the browser session already captured by ~chirp-login~; no separate media credential is required.  Temporary ciphertext is removed after success, failure, or cancellation.

** Delivery guarantees

Direct-message reads never send read acknowledgments.  Text, replies, reactions, and attachments are never retried or inserted optimistically.  Reply context and every composer object remain until X returns a validated acknowledgement.

* Keys

~chirp-home~ and ~chirp-following~ share one primary timeline buffer and retain independent posts, pagination, pending new posts, and positions.  Reopening either subview or switching with ~TAB~ restores canonical state without fetching again.  While the primary buffer is visible, Chirp checks the active feed every ~chirp-timeline-poll-interval~ seconds (30 by default); set the option to nil to disable automatic checks.  Fresh posts remain behind a centered, clickable ~Show N posts~ header-line button, which stays visible while the timeline scrolls and never moves the current row.  Scrolling within ~chirp-timeline-auto-load-threshold~ characters of the visible bottom quietly loads the next older page and preserves the current row; the default is 2000, and nil disables only automatic pagination.

| Key | Action |
|-----+--------|
| ~g~ | check immediately for new Home or Following posts without moving the current row.  Evil: ~g r~ |
| ~.~ | show pending new Home or Following posts and move to the newest entry |
| ~TAB~ | Home/Following, or profile subviews |
| ~n~ / ~p~ | next or previous entry.  Evil: ~g j~ / ~g k~ |
| ~N~ | older posts, conversations, or message history.  Evil: ~g n~ |
| ~q~ | close the window; Home and Following keep the buffer |
| ~RET~ / mouse-1 | Appkit action at point, or open the tweet or profile |
| ~m~ | first media item.  Evil: ~g m~ |
| ~D~ | download current media.  Evil: ~g d~ |
| ~A~ | author profile.  Evil: ~g a~ |
| ~S~ | add a spam phrase; ~C-u S~ starts from the display name.  Evil: ~g S~ |
| ~x~ | actions menu.  Evil: ~?~ |
| ~x r~ | reply, or refuse when X limits replies |
| ~x T~ | translate the tweet |
| ~o~ | open in a browser.  Evil: ~g o~ |


** Compose

| Key | Action |
|-----+--------|
| ~C-c C-a~ | attach images (up to 4) or one MP4 |
| ~C-c C-v~ | paste one clipboard image |
| ~C-c C-d~ | remove an attachment |
| ~C-c C-e~ | edit alt text |
| ~C-c C-n~ | commit the current part and start the next |
| ~C-c C-p~ | drop the current extra part |
| ~RET~ / ~e~ | load a draft row back into the composer |
| ~M-TAB~ | complete a handle after ~@~ |
| ~C-c C-s~ | save as an unsent X draft |
| ~C-c C-t~ | schedule for a local time |
| ~C-c C-c~ | send and keep the draft until X acknowledges |
| ~C-c C-k~ | cancel an in-flight submit, or close when idle |

Click ~Audience~ on a post or quote to choose who can reply.  Video
uploads show ~Uploading video n/N~ in the status strip.  ~x d~ opens
X drafts and ~x t~ opens scheduled posts.

JPEG, PNG, and WebP attachments may be at most 5 MiB; GIFs 15 MiB;
MP4 videos 512 MiB, uploaded in 1 MiB chunks.  Premium accounts can
exceed the standard 280 weighted-character limit.

* Notifications and translation

#+begin_src emacs-lisp
(chirp-notifications-mode 1)
(setq chirp-notifications-interval 300
      chirp-notifications-max-results 20
      chirp-translation-language "zh")
#+end_src

The first check establishes a baseline.  Later checks run every five
minutes by default.  Linux uses freedesktop notifications; macOS uses
AppleScript.

* Thread reply filtering

Chirp hides likely spam replies with a conservative default list.
Matching ignores case and never filters the focus tweet or its
ancestor chain.  Press ~S~ on a reply to save a literal phrase.
~M-x chirp-thread-edit-spam-rules~ edits the file under
~user-emacs-directory~.

#+begin_src emacs-lisp
(setq chirp-thread-spam-keywords nil) ; disable filtering
#+end_src

To propose rules for everyone, use the
[[https://github.com/LuciusChen/chirp/issues/new?template=spam-rule.yml][spam rule submission form]].

* Appearance

#+begin_src emacs-lisp
(setq chirp-tweet-separator "- - - - - - - - - - - -"
      chirp-tweet-separator-indent 6
      chirp-show-avatars nil
      chirp-show-tweet-media nil)
#+end_src

Timestamps follow X's localized forms through ~chirp-language~.
Edited posts show ~Edited · N versions~; activate it or use ~x H~.

* Media

** Timeline presentation

Images render as row-sliced previews.  Normal non-condensed posts—including
timelines, reposts, thread focuses, and thread replies—share one large carousel
display.  Quoted posts and other condensed cards retain X Web's center-cropped
~TweetPhotos~ grid.

Source aspect ratios determine the common carousel height.  Each item occupies
at most 80 percent of the reference width, 4-pixel gutters separate items, and
constrained boxes use center cover.  Static carousels use SVG.  Inline video
playback replaces the complete composite scene with one Canvas while preserving
the same geometry and keeping ~window-hscroll~ at zero.

** Actions

| Input             | Action                                                        |
|-------------------+---------------------------------------------------------------|
| ~RIGHT~ / ~LEFT~  | Select the next or previous carousel item                     |
| ~RET~             | Toggle inline video; open a still image in the media reader   |
| ~p~ (Evil)        | Toggle the selected inline video in normal or motion state    |
| mouse hotspot     | Activate the clicked carousel item without moving point       |
| ~m~               | Toggle the inline video's desired mute state                  |
| ~L~               | Toggle looping for the initialized selected inline video     |
| ~F~               | Open or focus an additional frame for the selected video     |
| ~C-RET~           | Present the selected item in the dedicated reader             |
| ~S-RET~           | Send the selected video to the configured external player     |
| ~M-RET~           | Bypass local actions and open the containing entry            |

~SPC~ is not a media action and remains available to the user's global key
configuration.

The transport keys are local to the media track at point.  Selecting another
carousel item retargets the commands; they never control the previous inline
occurrence.  ~F~ leaves the host view visible and does not start paused media.
Closing its frame changes no other window.  ~L~ does not initialize playback,
and enabling a loop requires a seekable source.

** Dedicated viewer

The dedicated buffer uses video.el's reader model.  Chirp does not override
~video-display-buffer-function~: the default placement is the selected window,
while users may globally choose another window or a presentation frame.

Cold-cache photos are acquired asynchronously before the reader opens.  The
request belongs to the exact Chirp source buffer: closing that buffer,
replacing its feed, or canceling pending reader navigation prevents a late
download from opening another viewer.

The buffer presents the current source; every window displaying it owns an
independent absolute scale and viewport origin.  A video's Appkit session may
also retain the same player for an inline surface.  The virtual media plane may
be much larger than the window, while its Canvas and native raster stay exactly
viewport-sized.  Fit commands calculate a scale once, so later window resizing
does not resize the media.

Images and videos support zoom, unrestricted pan, fit, mouse-wheel input, and
middle-button dragging.  Videos additionally expose playback transport.
Vanilla ~n~/~p~ and Evil ~gj~/~gk~ move through the source carousel; Evil ~p~
toggles playback.  In an ordinary reader ~q~ returns to the saved Chirp view;
inside an independent presentation frame it closes only that frame.

Returning to the same window with unchanged layout leaves the inline media
track intact: playback continues, or remains paused, with its mute state
unchanged. Actual source-width or text-scale changes still reflow the source
rows; preserving inline playback through that reflow is not yet supported.

Appkit owns each activated video's canonical resource, stable cache identity,
and persistent-cache policy; video.el owns the playback session, player, and
inline/dedicated presentation leases.  Chirp records the active inline
presentation against its containing entry, so every dedicated-media action
reuses the same player even after point leaves the media hot spot.  The reader
preserves the exact position, play/pause and canonical mute state, buffered
ranges, and in-flight sparse network cache without issuing a second media
request.  Closing either video presentation leaves the other lease running;
closing the last lease lets video.el retire the player and any incomplete
session cache.  Closing the owning Chirp source also closes its managed
dedicated reader.  Explicit external playback and download remain separate.

** Configuration

#+begin_src emacs-lisp
(setq chirp-media-thumbnail-size 256
      chirp-video-player-window-size '(1280 . 720)
      chirp-media-download-directory "~/Downloads/chirp/"
      chirp-media-prefetch-images nil
      chirp-backend-read-cache-ttl 15)
#+end_src

Set ~chirp-video-player-command~ to another player, or ~nil~ to open externally
requested videos in the browser.
